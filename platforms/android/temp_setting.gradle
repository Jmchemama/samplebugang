android {
            defaultConfig {
                applicationId = "org.nativescript.samplebugang"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }